<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/listView_padding"
    android:background="@drawable/bg_item"
    android:orientation="vertical"
    android:padding="@dimen/listView_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/sectionName_tv"
            style="@style/ListItemTV"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/live_iv"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:contentDescription="@string/no_image"
            android:src="@drawable/ic_brightness_24dp"
            android:visibility="gone" />

    </LinearLayout>

    <TextView
        android:id="@+id/webTitle_tv"
        style="@style/ListItemTV.Title" />

    <TextView
        android:id="@+id/webPubDate_tv"
        style="@style/ListItemTV" />

</LinearLayout>